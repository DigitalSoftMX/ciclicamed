import {
    defineComponent
} from '@vue/runtime-core';

export default defineComponent({
    props: {
        title: {
            type: Number,
            default: 0
        }
    },
    data() {
        return {
        };
    },
    mounted() {
    },
    methods: {
    },
})
