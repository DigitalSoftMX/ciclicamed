import { PaginationData } from "@data/General/Pagination.data";
import { CheckupPagination } from "@interface/Checkup/CheckupPagination.interface";

export const CheckupPaginationData: CheckupPagination = {
    pagination: PaginationData,
    data: []
}